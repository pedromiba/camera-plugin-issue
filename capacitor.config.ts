import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.camera.plugin.issue',
  appName: 'camera-plugin-reproduction-issue',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
